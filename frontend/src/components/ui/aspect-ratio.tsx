import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

// Mantiene una relación de aspecto fija (ej. 16:9) en su contenido,
// independiente del ancho disponible. Es solo un alias directo del
// componente de Radix UI, sin estilos ni lógica adicional.
const AspectRatio = AspectRatioPrimitive.Root;

export { AspectRatio };
